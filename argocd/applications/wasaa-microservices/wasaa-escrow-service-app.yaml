# wasaa-escrow-service-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wasaa-escrow-service-developer
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: wasaa-microservices
  sources:
    - repoURL: https://github.com/web-masters-ke/wasaa-chat-helm-charts.git
      targetRevision: main
      path: wasaa-escrow-service
      helm:
        releaseName: wasaa-escrow-service
        values: |-
          replicaCount: 1
          replicas: 1
  destination:
    server: https://kubernetes.default.svc
    namespace: wasaa-escrow-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
